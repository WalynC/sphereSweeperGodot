shader_type canvas_item;

const float lod = 0.0;
//sampler2D SCREEN_TEXTURE: hint_screen_texture, filter_linear_mipmap;

void fragment(){
	//vec4 color = texture(, SCREEN_UV, lod);
	//COLOR = vec4(color.rgb, 1.0);
}